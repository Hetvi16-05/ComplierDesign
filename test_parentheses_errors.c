#include <stdio.h>

int main() {
    int x = 5;
    
    // Test case: for() - should detect empty for loop
    for()
    
    // Test case: for( then } - missing closing parenthesis
    for(
    
    // Test case: if (x > 0 then } - missing closing parenthesis
    if (x > 0
    
    return 0;
}
