#include <stdio.h>

int main() {
    int x = 5;
    
    // Empty parentheses
    for()
    
    // Missing closing parenthesis
    if (x > 0
    
    // Missing opening parenthesis  
    x > 0)
    
    // Extra closing parenthesis
    if (x > 0))
    
    return 0;
}
